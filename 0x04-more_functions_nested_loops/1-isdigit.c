/**
 * _isdigit - checks for numbers from 0 to 9
 * @c: takes an integeter input
 *
 * Return: 1 if is within the range else return 0
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
