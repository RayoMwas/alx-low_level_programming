/**
 * mul - multiplies two integers
 * @a: takes the 1st integer
 * @b: takes the 2nd integer
 *
 * Return: the result of multiplying
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
