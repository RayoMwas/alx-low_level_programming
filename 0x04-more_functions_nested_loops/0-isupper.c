/**
 * _isupper - checks for uppercase characters
 * @c: Takes a charcater input
 *
 * Return: 1 if uppercse else return 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
